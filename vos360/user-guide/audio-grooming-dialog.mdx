---
title: Audio grooming dialog
robots: noindex, nofollow
---

Review the audio grooming dialog for options available for your services.

**Audio grooming dialog**

![vos360\_audio\_grooming.png](/images/vos360/55452caa0c11865d4542932a.png)

<div className="vos-table-scroll">
  <table className="vos-source-table">
    <colgroup span="1"><col span="1" /><col span="1" /></colgroup>

    <tbody><tr><td colSpan="1" rowSpan="1"><p><span>Stream name</span></p></td><td colSpan="1" rowSpan="1"><p>Specify a custom textual description of the content of the audio track.</p><p>This textual description is expected to be used in the client application UI (player UI) for audio stream selection, in addition to the existing Language configured. It is useful to differentiate the audio tracks with the same language configured but for different purposes (e.g. "Audio description for the visually impaired") or audio tracks with no language (e.g. "Instrumental").</p><p>Note that this value can be set in any language, i.e. any UTF-8 character string (including space characters).</p><p>The display of the custom textual description in the player depends on the support by the player of the methods described below for DASH and HLS:</p><ul><li><p>{"For DASH/CMAF-DASH package output, it will be inserted as <Label> sub-element of the corresponding audio <AdaptationSet> in the DASH manifest. Supported for Live, Start-Over, Catch-Up, and VOD services."}</p></li></ul><blockquote><p><strong>Note: </strong><span>{"By default no <Label> element is inserted in the DASH manifest."}</span></p><p><span>To enable adding the Stream Name in the manifest, configure the following settings in the destination profile via LabWizard REST API:</span></p><ul><li><p>For DASH package output, enable "<span>insertLabelElement</span>" in DASH packaging properties.</p></li><li><p>For CMAF-DASH package output, enable "dashInsertCustomStreamName" in CMAF packaging properties (supported for Live packaging only)</p></li></ul></blockquote><p /><ul><li><p>For <span>HLS/CMAF-HLS package output</span>, it will be inserted as "NAME" attribute of the #EXT-X-MEDIA tag of the corresponding audio tracks. Supported for Live, Start-over, Catch-up, Long-lasting Catch-up, and VOD services.  </p></li></ul><blockquote><p><strong>Note: </strong><span>By default the NAME attribute is based on the ISO639 language code or, for Live, the automatic naming option can be enabled (ISO639 language code converted to the language name in native language or English).</span></p><p><span>To enable adding the Stream Name in the manifest, configure the following settings in the destination profile via LabWizard REST API:</span></p><ul><li><p>For HLS package output, enable "insertCustomStreamName" in HLS packaging properties.</p></li><li><p>For CMAF-HLS package output, enable "hlsInsertCustomStreamName" in CMAF packaging properties.</p></li></ul></blockquote><blockquote><p><strong>Note: </strong>Leave the Stream Name field blank and/or do not configure the HLS/DASH/CMAF insertion settings if you want VOS to use the default audio track naming.</p></blockquote></td></tr><tr><td colSpan="1" rowSpan="1"><p>Language</p></td><td colSpan="1" rowSpan="1"><p>Configure the audio language.</p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Level (dbFS)</p></td><td colSpan="1" rowSpan="1"><p>Configure the audio level. The default value is -<span>24 dBFS.</span></p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Type</p></td><td colSpan="1" rowSpan="1"><p>Configure the audio type.</p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Delay (ms)</p></td><td colSpan="1" rowSpan="1"><p>Specify the audio delay in milliseconds in order to synchronize with the video stream. The valid range is from -500 to 500 ms. </p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Nielsen</p></td><td colSpan="1" rowSpan="1"><p>Enable / disable Nielsen ID3 watermarks (used to measure live and on-demand viewing on multiple device types).</p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Default</p></td><td colSpan="1" rowSpan="1"><p>Indicate the default stream.</p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Silence Threshold (ms)</p></td><td colSpan="1" rowSpan="1"><p>Specify the silence threshold in milliseconds. If the silence threshold is met, the audio fallback will be triggered and the alarm will be raised. </p><blockquote><p><strong>Info: </strong>Configure the audio fallback settings in the IPTV Profile/Multiscreen Profile<strong> </strong>as follows:</p><ul><li><p>Set up the Audio with "<em>Match by Source Labe</em>l" mode selected.</p></li><li><p>Create at least 2 unique audio tracks that must be configured with each with its unique Source Label.</p></li><li><p>Configure the "<strong><span>Fallback source label</span></strong><span>"</span> and "<strong><span>Fallback on Silence</span></strong><span>".</span> (Refer to  <a href="/vos360/user-guide/broadcast-profile-parameters">/wiki/spaces/VOS360DOCC/pages/167384893</a>/<a href="/vos360/user-guide/streaming-profile-parameters">/wiki/spaces/VOS360DOCC/pages/167384897</a> for details.)</p></li></ul></blockquote></td></tr><tr><td colSpan="1" rowSpan="1"><p>Keep metadata</p></td><td colSpan="1" rowSpan="1"><p><span>Keep the metadata in the audio stream for the AC3 (Dolby Digital) and E-AC3 (Dolby Digital Plus) stream types.</span></p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Ignore?</p></td><td colSpan="1" rowSpan="1"><p>Enable / disable the stream.</p></td></tr><tr><td colSpan="1" rowSpan="1"><p>Force Atmos</p></td><td colSpan="1" rowSpan="1"><p><span>If the detected audio stream type is E-AC3 (Dolby Digital Plus), the checkbox will be enabled.</span><br /><br /><span>In case an E-AC3 audio track is dynamically switching between different audio modes (stereo, 5.1, Atmos), this checkbox allows to force signaling this audio track as Atmos audio in HLS and DASH packaged outputs (instead of signaling what VOS360 detected at the service start).</span></p></td></tr><tr><td colSpan="1" rowSpan="1"><p>CHANNELS<br />(For <strong>REST API</strong> configurations only)</p></td><td colSpan="1" rowSpan="1"><p><span>This contains the number of audio channels for the specific track. </span></p><blockquote><p><strong>Note: </strong>The number of audio channels is contained in the CHANNELS playlist item. The presence of this item in the HLS master playlist is conditioned by the "InsertAudioChannels" parameter in the Destination profile. This parameter is visible and can be modified only in the REST API. On the VOS 1.20 or later created HLS and CMAF Destination profiles, it is enabled by default. </p><p>When present, the item contains:</p><ul><li><p>In Mono, Stereo, multi-channel (5.1): the number of channels ("1", "2" or "6")</p></li><li><p>In Atmos (5.1.4): Dolby Atmos Complexity_Index / Audio Coding ("12/JOC")</p><ul><li><p>The Complexity Index is a number representing the number of channels in the base EAC-3 layer + objects in the Atmos layer. In VOS, it is always 12.</p></li><li><p>The Audio Coding is JOC  (Joint Object Coding), the technology behind the EAC-3 + enhancement Atmos layer that VOS uses.</p></li></ul></li></ul></blockquote></td></tr></tbody>
  </table>
</div>
