---
title: Configure the audio encoding parameters
sidebarTitle: Configure audio encoding
---

<span id="_top" className="mk-legacy-anchor" />

{ /* TODO: update links (especially ./parameters/) once MD files are created */}

Audio encoding parameters are only available if you defined an audio input from the **Input** tab.

1. Click **Add** to create a new audio encoding, or click <img src="/icons/ui/edit.svg" alt="Edit" className="mk-ui-icon" width="16" height="16" /> to edit an existing one. The **Encoding audio** window displays.

2. Select your audio **Codec** in the drop-down list. Depending on the selected audio codec, different parameters may display.

   <Info>
     **Pass-Through** lets you receive an already encoded stream and forward it to the output.
   </Info>

3. Enter the appropriate value for [audio encoding parameters](/beam/live-encoder/parameters/encoding/audio).

4. Click **Save and continue** or **Save and exit** to save your changes.
