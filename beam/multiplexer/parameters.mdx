---
title: Notes about Multiplexer parameters
sidebarTitle: Configuration parameters
---

<span id="_top" className="mk-legacy-anchor" />

Multiple definitions may exist for the same parameter name. For example, parameters may have the same name but are used in different configuration contexts.

All Parameters followed by an asterisk (\*) in the UI are mandatory.

Default values, if any exist, are already supplied in the UI.
